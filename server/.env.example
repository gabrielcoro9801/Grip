# Copia questo file in .env (non versionato) e adatta se necessario.
# Credenziali di sviluppo locale create durante lo scaffolding iniziale
# (utente applicativo dedicato, non il superuser postgres).
DATABASE_URL=postgres://grip_app:grip_dev_password@localhost:5432/grip_dev
PORT=3001
